import { ReactNode } from "react";

export type PlaceProps = {
  title: ReactNode;
  subtitle: ReactNode;
  address: ReactNode;
  description: ReactNode;
  label: ReactNode;
  page: string;
  url: string;
}
